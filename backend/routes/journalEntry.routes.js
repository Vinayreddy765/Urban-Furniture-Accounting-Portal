const router=require('express').Router(); const {body}=require('express-validator'); const validate=require('../middleware/validate'); const {authenticate,authorize}=require('../middleware/auth'); const ctrl=require('../controllers/journalEntry.controller');
router.use(authenticate,authorize('Administrator','Accountant')); router.get('/',ctrl.list); router.get('/:id',ctrl.getById); router.post('/',[body('journalId').isInt({min:1}),body('entryDate').isISO8601(),body('lines').isArray({min:2}),body('lines.*.accountId').isInt({min:1}),body('lines.*.debit').optional({nullable:true}).isFloat({min:0}),body('lines.*.credit').optional({nullable:true}).isFloat({min:0}),body('lines.*.analyticAccountId').optional({nullable:true}).isInt({min:1})],validate,ctrl.create); module.exports=router;
